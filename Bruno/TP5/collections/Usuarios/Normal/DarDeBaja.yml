info:
  name: DarDeBaja
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:7128/api/usuarios/dardebaja
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
