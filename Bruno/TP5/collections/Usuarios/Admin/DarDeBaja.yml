info:
  name: DarDeBaja
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:7128/api/usuarios/admin/dardebaja/{idUsuario}
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
